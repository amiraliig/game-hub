import React, { useEffect } from 'react'

const ColorMode = () => {
    useEffect(() => {
        const root = window.document.documentElement;
        // root.classList.add('');
    })
    return (
        <div>ColorMode</div>
    )
}

export default ColorMode